## No Spec Changes

Este change es puramente correctivo (bugfix). No introduce nuevas capabilities ni modifica requisitos existentes. Los specs de los cambios `auth-backend` y `backend-config` no se alteran — el comportamiento esperado sigue siendo el mismo, solo se corrigen bugs de implementación que impedían el startup.
